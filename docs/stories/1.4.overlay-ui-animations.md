# 1.4 Overlay UI & Animations

## Статус: ✅ Завершено (базовая реализация)

### Что реализовано (01.02.2026)

- ✅ TypeScript types для всех состояний UI (OverlayState, VoiceState, StateMetadata)
- ✅ Компонент StateIndicator с поддержкой 5 состояний
- ✅ CSS анимации для всех переходов (pulse, ripple, spin, shake, wave)
- ✅ Компонент TooltipManager с hotkey hints
- ✅ IPC API для синхронизации voice states
- ✅ MCPIntegration класс для polling MCP server
- ✅ Автоматическое обновление UI при изменении voice state
- ✅ Waveform visualization для recording state
- ✅ Платформо-зависимое отображение hotkeys (⌘ на macOS, Ctrl на Windows)

### Что осталось сделать

- [ ] Реальная интеграция с MCP server API (сейчас используется mock)
- [ ] Unit тесты для UI компонентов
- [ ] E2E тесты для state transitions

## Описание

Реализация визуальных состояний и анимаций для overlay окна. Портирование компонентов UI из браузерной версии в Electron overlay с поддержкой 5 состояний, анимаций переходов и tooltips с hotkey hints.

## Ключевые сложности

- **5 состояний UI** (⭐⭐) - idle, listening, recording, processing, error с плавными переходами
- **Анимации и переходы** (⭐⭐) - Визуальная обратная связь для каждого состояния
- **Tooltips с hotkey hints** (⭐) - Динамическое отображение горячих клавиш
- **Интеграция с MCP server** (⭐⭐⭐) - Синхронизация состояний между overlay и MCP server

## Зависимости

- ✅ Story 1.2 - Window Manager & Overlay (завершено)
- ✅ Story 1.3 - Global Hotkey Registration (завершено)

## Требования

### 1. UI States System

#### 1.1 Определение состояний
- [ ] **idle** - Готов к работе, ожидание действий пользователя
  - Индикатор: зелёная точка
  - Текст: "Ready"
  - Анимация: плавное пульсирование

- [ ] **listening** - Активное прослушивание голоса (после нажатия hotkey)
  - Индикатор: синяя пульсирующая точка
  - Текст: "Listening..."
  - Анимация: быстрое пульсирование, ripple эффект

- [ ] **recording** - Идет запись голоса (voice input detected)
  - Индикатор: красная точка
  - Текст: "Recording..."
  - Анимация: sound wave визуализация

- [ ] **processing** - Обработка голосового ввода
  - Индикатор: жёлтая/оранжевая вращающаяся точка
  - Текст: "Processing..."
  - Анимация: spinner/loader

- [ ] **error** - Ошибка (нет микрофона, проблема с API и т.д.)
  - Индикатор: красная точка (без пульсации)
  - Текст: "Error: [message]"
  - Анимация: shake эффект при появлении ошибки

#### 1.2 State Machine
```typescript
enum OverlayState {
  IDLE = 'idle',
  LISTENING = 'listening',
  RECORDING = 'recording',
  PROCESSING = 'processing',
  ERROR = 'error'
}

interface StateTransition {
  from: OverlayState;
  to: OverlayState;
  trigger: string; // hotkey, voice-detected, processing-complete, error
}
```

### 2. Визуальные компоненты

#### 2.1 Status Indicator
- [ ] Цветная точка с анимацией для каждого состояния
- [ ] Плавные переходы между цветами (CSS transitions)
- [ ] Динамические анимации (pulse, ripple, wave, spinner, shake)

#### 2.2 Status Text
- [ ] Текстовое описание текущего состояния
- [ ] Fade-in/fade-out при изменении текста
- [ ] Поддержка многострочного текста для error messages

#### 2.3 Waveform Visualization (для recording state)
- [ ] Визуализация sound wave при записи
- [ ] Использование Web Audio API или CSS-based анимация
- [ ] Responsive к уровню громкости (опционально)

### 3. Animations & Transitions

#### 3.1 CSS Animations
```css
/* Pulse Animation (idle, listening) */
@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(1.1); }
}

/* Ripple Effect (listening) */
@keyframes ripple {
  0% { transform: scale(1); opacity: 1; }
  100% { transform: scale(2.5); opacity: 0; }
}

/* Spinner (processing) */
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Shake (error) */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-10px); }
  75% { transform: translateX(10px); }
}

/* Wave (recording) */
@keyframes wave {
  0%, 100% { height: 4px; }
  50% { height: 20px; }
}
```

#### 3.2 State Transition Animations
- [ ] Fade transitions между состояниями (200-300ms)
- [ ] Scale animations для акцентирования изменений
- [ ] Color transitions для индикатора

### 4. Tooltips с Hotkey Hints

#### 4.1 Tooltip Component
- [ ] Показать tooltip при hover над элементами
- [ ] Отображение горячих клавиш в tooltip
- [ ] Платформо-зависимое отображение клавиш (⌘ на macOS, Ctrl на Windows/Linux)

#### 4.2 Hotkey Display
```typescript
interface HotkeyHint {
  action: string; // "Toggle Overlay", "Start Recording"
  keys: string[]; // ['Command', 'Shift', 'Space'] или ['Globe']
  platformSpecific: boolean;
}
```

- [ ] Форматирование hotkey для отображения (e.g., "⌘⇧Space" или "Globe Key")
- [ ] Динамическая подгрузка hotkey из конфигурации

#### 4.3 Tooltip Positioning
- [ ] Умное позиционирование (не выходить за границы экрана)
- [ ] Offset от курсора
- [ ] Fade-in/fade-out анимация (150ms)

### 5. Интеграция с MCP Server

#### 5.1 IPC Events для состояний
```typescript
// Renderer -> Main
interface VoiceStateUpdate {
  state: OverlayState;
  metadata?: {
    errorMessage?: string;
    recordingDuration?: number;
    volume?: number;
  };
}

// Main -> Renderer
interface VoiceStateChanged {
  state: OverlayState;
  timestamp: number;
  metadata?: any;
}
```

#### 5.2 MCP Server Integration
- [ ] Подключение к MCP server через HTTP/WebSocket
- [ ] Получение состояния voice input (listening, recording, processing)
- [ ] Обновление UI в реальном времени
- [ ] Обработка ошибок соединения

### 6. Accessibility & UX

#### 6.1 Keyboard Navigation
- [ ] Tab navigation между элементами управления
- [ ] Focus indicators
- [ ] Keyboard shortcuts для управления

#### 6.2 Visual Feedback
- [ ] Hover states для всех интерактивных элементов
- [ ] Active states для кнопок
- [ ] Disabled states (когда функции недоступны)

#### 6.3 Error Handling & User Feedback
- [ ] Понятные сообщения об ошибках
- [ ] Автоматическое скрытие error state после timeout (5-10 секунд)
- [ ] Retry механизм для recoverable errors

## Технический стек

### CSS/Styling
- CSS3 Animations (keyframes, transitions)
- CSS Grid/Flexbox для layout
- CSS Custom Properties (variables) для theming

### TypeScript/JavaScript
- State Machine для управления состояниями
- Event listeners для IPC
- Typed interfaces для type safety

### Electron IPC
- `ipcRenderer.send()` для отправки событий из renderer в main
- `ipcMain.handle()` для обработки запросов в main process
- `webContents.send()` для отправки обновлений из main в renderer

## Файловая структура

```
electron/
├── renderer/
│   ├── index.html          # UI markup
│   ├── styles.css          # Styling + animations
│   ├── main.ts            # Renderer logic
│   └── components/        # (new)
│       ├── StateIndicator.ts
│       ├── Tooltip.ts
│       └── Waveform.ts    # (опционально)
├── main/
│   ├── window-manager.ts  # Управление окном
│   └── mcp-integration.ts # (new) MCP server integration
└── preload/
    └── index.ts           # IPC bridge
```

## Макет UI

```
┌─────────────────────────────────┐
│ 🎙️ Voice Hooks          📌 ⚙️  │ <- Drag handle + controls
├─────────────────────────────────┤
│                                 │
│   ● Ready                       │ <- Status indicator + text
│                                 │
│   [Waveform visualization]      │ <- (только в recording state)
│                                 │
└─────────────────────────────────┘

Tooltips (on hover):
📌 "Always on Top (⌘T)"
⚙️ "Settings"
🎙️ "Toggle Listening (Globe)"
```

## Критерии приемки

- [ ] 5 состояний UI реализованы и работают корректно
- [ ] Плавные анимации переходов между состояниями
- [ ] Tooltips отображаются при hover с правильными hotkey hints
- [ ] Интеграция с MCP server для получения voice states
- [ ] Визуальная обратная связь для всех действий пользователя
- [ ] Error handling с понятными сообщениями
- [ ] Кросс-платформенное отображение hotkeys (macOS, Windows, Linux)
- [ ] Accessibility: keyboard navigation, focus indicators
- [ ] Тесты для state transitions (unit tests)
- [ ] Документация для разработчиков

## Следующие шаги (Story 1.5)

После завершения Story 1.4:
1. Settings Panel - настройки голосового ввода, hotkeys, UI preferences
2. System Tray Integration - minimize to tray, tray menu
3. Notifications - desktop notifications для важных событий

## Команды для работы

```bash
# Development с hot reload
npm run electron:dev

# Build production
npm run electron:build

# Запуск MCP server (для тестирования интеграции)
npm start
```

## Риски и Митигация

### Риск 1: MCP server connection issues
**Проблема:** Overlay может потерять соединение с MCP server

**Митигация:**
- Автоматический reconnect с exponential backoff
- Fallback в idle state при потере соединения
- Визуальный индикатор проблем с соединением

### Риск 2: Performance анимаций
**Проблема:** Анимации могут тормозить на слабых машинах

**Митигация:**
- Использовать CSS transforms (GPU-accelerated)
- Опция отключения анимаций в настройках
- Profiling и оптимизация

### Риск 3: Сложность waveform visualization
**Проблема:** Web Audio API может быть сложным для реализации

**Митигация:**
- Начать с простой CSS-based анимации
- Waveform как optional enhancement
- Альтернатива: animated bars вместо реального waveform

## Примечания

- Дизайн должен быть минималистичным и не отвлекать от работы
- Анимации должны быть быстрыми (100-300ms) для responsiveness
- Tooltips должны появляться с небольшой задержкой (500ms) чтобы не мешать
- Состояние error должно автоматически исчезать после timeout или user action
